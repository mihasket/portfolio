{{define "title"}}Home{{end}}

{{define "main"}}
    <div class="homeMainDiv">
        <div class="flex-center-100">
            <div class="terminal">
                {{ template "information" . }}
            </div>
        </div>
        <div class="flex-center-100">
            <div class="terminal">
                {{ template "zsh" "pfetch" }}
<pre>   __
-=(o '.       miha@mp3
   '.-.\      os      Golang
   /|  \\     host    mihasket
   '|  ||     ascii   snd
    _\_):,_   bar     waybar
</pre>
                {{ template "zsh" "" }}
            </div>
        </div>
    </div>
{{end}}
